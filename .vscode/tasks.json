{
    "tasks": [
        {
            "type": "shell",
            "label": "clang build",
            "command": "/usr/bin/clang",
            "args": [
                "-g",
                "main.c",
                "-lSDL2",
                "-lm",
                "-o",
                "boulder-dash.out"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ],
    "version": "2.0.0"
}